# Static site: Không build, publish toàn bộ repo
[build]
  publish = "."  # Publish thư mục gốc (tất cả files HTML/CSS/JS)
  command = ""   # Không chạy build command

# Headers (tùy chọn, giữ nếu cần bảo mật)
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"